import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Manrope } from "next/font/google";



export const metadata: Metadata = {
  title: 'Pitch Lab | Precision Soccer Training Gear',
  description: 'Elevate your game with Pitch Lab\'s laboratory-engineered soccer accessories. Designed for players who demand pro-level performance and precision.',
  keywords: ["soccer training, soccer gear, performance soccer, agility training, goalie gloves, soccer accessories"],
  openGraph: {
    "title": "Pitch Lab | Soccer Gear Elevated",
    "description": "Professional training gear engineered for performance.",
    "siteName": "Pitch Lab"
  },
};

const manrope = Manrope({
  variable: "--font-manrope",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${manrope.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
